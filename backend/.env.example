JWT_SECRET=change-me-in-production

# Leave unset for local SQLite dev. Set for Fly Postgres / Supabase.
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Set to false only for local Postgres without SSL
# DATABASE_SSL=false

PORT=4000
