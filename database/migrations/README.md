# Archived migrations

These SQL files describe an **older** schema (predefined `segments` table, integer ratings). They are **not** used by the current Stravo backend.

The live schema is defined by Sequelize models in `backend/src/models/` and reference SQL in `database/schema.sql`.

Do not run these migrations against a database used by the current app.
